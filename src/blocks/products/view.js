/**
 * Products Block View
 */
import { store, getContext } from '@wordpress/interactivity';

store('meiliRivera/search', {
    actions: {
        // Actions are already defined in global store.js, 
        // but we can add block-specific logic here if needed.
    }
});

// We can also subscribe to changes or init things here specifically for this block.
